{
  "name": "komodo-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "check:release": "node scripts/check-release.mjs",
    "test": "pnpm check:release && pnpm typecheck && pnpm -r --if-present test",
    "build:release": "pnpm -C apps/web build && pnpm -C packages/cli build"
  }
}
