# `next dev` opens an empty store on a fresh clone; the sample queue is what
# makes the app explorable before an ingester has ever run. Only loaded by
# `next dev` — a production start, including the one `komodo serve` spawns,
# never reads this file.
KOMODO_SEED=1
