node_modules/
dist/
.komodo/
*.log
.DS_Store
.env
.env.*
!.env.example
# Committed on purpose: it carries no secret, only the dev-only decision to
# seed the sample queue. Next loads it for `next dev` and nothing else.
!.env.development
coverage/
.turbo/
.next/
.astro/
.gstack/

# Build artefacts and agent scratch state
*.tsbuildinfo
apps/*/.omc/
packages/*/.omc/

*.tgz

.npmrc