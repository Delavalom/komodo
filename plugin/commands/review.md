---
description: Review the current branch with Komodo and open the queue
---

Run the `komodo-review` skill on the current branch.

If the user passed arguments, treat them as the base branch to diff against
and pass them through as `--base`.
